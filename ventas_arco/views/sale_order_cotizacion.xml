<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="sale_order_cotizacion_wizard_form" model="ir.ui.view">
            <field name="name">sale.order.cotizacion.wizard.form</field>
            <field name="model">sale.order.cotizacion</field>
            <field name="arch" type="xml">
                <form string="Orden de venta">
                    <group>
                      <p>Se creara una cotizacion con todas las tareas seleccionadas</p>
                    </group>

                    <footer>
                        <button string="Crear orden de venta"
                            name="create_report_button" type="object" class="btn-primary"/>
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="add_to_cotizacion" model="ir.actions.server">
            <field name="name">Añadir a orden de venta</field>
            <field name="state">code</field>
            <field name="type">ir.actions.server</field>
            <field name="model_id" ref="model_project_task"/>
            <field name="binding_model_id" ref="model_project_task"/>
            <field name="binding_view_types">list</field>
            <field name="code">
                if records:
                    action = records.action_add_to_cotizacion_view()
            </field>
        </record>

    </data>

</odoo>